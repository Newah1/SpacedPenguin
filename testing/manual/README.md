# Manual browser harnesses

These pages are focused interactive diagnostics, not part of the automated Playwright suite. Start the repository server with `npm run serve`, then open:

`http://127.0.0.1:4173/testing/manual/`

Each harness declares the repository root as its HTML base so production modules and assets continue to resolve from their canonical paths.
