# Archived manual levels

These are the previous hand-authored campaign levels. They are retained as an alternate catalog and are not loaded by default.

Load one with `?level=manual:NUMBER`, for example `http://localhost:8000/?level=manual:5`. The catalog contains levels 1 through 20, and advancing stays within it.
